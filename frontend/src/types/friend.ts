// Shared types for friends and users

export interface Friend {
    id: number;
    full_name: string;
    email: string;
}

export interface User {
    id: number;
    full_name: string;
    email: string;
}
