# Fly.io Configuration for Splitwiser
# Deploy with: fly deploy

app = 'splitwiser'
primary_region = 'sjc'

[build]
  dockerfile = "Dockerfile.fly"

[build.args]
  # Google OAuth client ID (safe to commit - this is public)
  VITE_GOOGLE_CLIENT_ID = "411308177468-dhc3vp78shfq5efb5nnrne56kl8a9iob.apps.googleusercontent.com"

[env]
  CLIENT_URL = "https://splitwiser.onlyslop.dev"
  DATABASE_PATH = "/data/db.sqlite3"
  DATA_DIR = "/data"
  # Google OAuth client ID for backend verification
  GOOGLE_CLIENT_ID = "411308177468-dhc3vp78shfq5efb5nnrne56kl8a9iob.apps.googleusercontent.com"

[http_service]
  internal_port = 8080
  force_https = true
  # Keep one machine up at all times: SQLite on a mounted volume means a
  # cold start is a real wait, and the PWA's background sync expects the API
  # to answer without one.
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

[[http_service.checks]]
  grace_period = "30s"
  interval = "30s"
  method = "GET"
  path = "/api/docs"
  timeout = "10s"

[mounts]
  source = "splitwiser_data"
  destination = "/data"

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
